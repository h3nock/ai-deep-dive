# Rate limiting zones for judge endpoints
# Apply in http {} context (included via /etc/nginx/nginx.conf or conf.d)
limit_req_zone $binary_remote_addr zone=submit:10m rate=1r/s;
limit_req_zone $binary_remote_addr zone=result:10m rate=5r/s;
limit_conn_zone $binary_remote_addr zone=conn:10m;
